{
    organization(login: "{{ orgName }}") {
        id
        projectV2(number: {{ projectNumber }}) {
            id
            title
            number
            createdAt
            closedAt
            closed
            shortDescription
            public
            readme
            url
        }
    }
}
